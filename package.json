{
  "name": "linktree-clone",
  "private": true,
  "scripts": {
    "dev:all": "concurrently -n backend,frontend -c blue,green \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "build": "echo 'Build will be done during start'",
    "start": "cd frontend && npm install && npm run build && cd ../backend && npm start"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
