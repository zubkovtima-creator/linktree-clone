# Linktree Clone

Дипломный проект — клон Linktree.

## Структура

- `backend/` — API (Node.js + Express)
- `frontend/` — клиент (React + Vite)

## Запуск

```bash
# Backend
cd backend
npm install
npm run dev

# Frontend
cd frontend
npm install
npm run dev
```
